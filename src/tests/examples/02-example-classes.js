// TODO: Think about more example or even change the exercise
describe('My module', () => {
  beforeAll(() => {
    // mock functions
    // reset smth
    // init smth
    // jest.resetAllMocks() or jest one
    // jest.restoreAllMocks() or jest one
  })

  beforeEach(() => {
  })

  afterAll(() => {
  })

  afterEach(() => {
  })
})
